export interface Vector3D {
    x: number;
    y: number;
    z: number;
}

export interface NumberLimits {
    min: number;
    max: number;
}

export interface Vector3DLimits {
    x: NumberLimits;
    y: NumberLimits;
    z: NumberLimits;
}
